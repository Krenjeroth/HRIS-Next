/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
    // modern: true,
    // modularize: true,
    // css: true,
  },

  // reactStrictMode: false,
};

module.exports = nextConfig;
