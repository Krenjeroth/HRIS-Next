/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  experimental: {
    appDir: true,
    // modern: true,
    // modularize: true,
    // css: true,
  },

  // reactStrictMode: false,
};

module.exports = nextConfig;
